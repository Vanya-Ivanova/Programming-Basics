function concatenateData(a1, a2, a3, a4) {
    let firstName = a1;
    let lastName = a2;
    let age = a3;
    let town = a4;

    console.log(" You are " + firstName + " " + lastName + ", a " + age + "-years old person from " + town + ".");
}